(()=>{
  const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
  const PREF='estoque-v2-card-prefs';
  const COLORS={yellow:'#ffe11a',red:'#ff2737',green:'#18c976',blue:'#3b82f6',purple:'#a855f7',orange:'#f97316',cyan:'#06b6d4',pink:'#ec4899'};
  const METRICS={pending_boxes:'Caixas a produzir',produced_boxes:'Caixas produzidas',production_progress:'Progresso da produção',open_orders:'Pedidos abertos',partial_items:'Itens parciais',overdue_orders:'Pedidos atrasados',receivable_amount:'Valor a receber',debtor_customers:'Clientes devendo',overdue_amount:'Valor vencido',finance_progress:'Progresso financeiro'};
  const DEFAULTS={production:{id:'production',title:'PRODUÇÃO',primary:'pending_boxes',secondary:'production_progress',period:'current',color:'yellow',visible:true,target:'orders',moneyHidden:false},finance:{id:'finance',title:'FINANCEIRO',primary:'receivable_amount',secondary:'finance_progress',period:'current',color:'red',visible:true,target:'finance',moneyHidden:false}};
  let cfg=load(),editing=null;
  function load(){try{const x=JSON.parse(localStorage.getItem(PREF)||'{}');return {production:{...DEFAULTS.production,...x.production},finance:{...DEFAULTS.finance,...x.finance},custom:Array.isArray(x.custom)?x.custom:[]}}catch{return {production:{...DEFAULTS.production},finance:{...DEFAULTS.finance},custom:[]}}}
  function save(){localStorage.setItem(PREF,JSON.stringify(cfg))}
  function inPeriod(value,period){if(period==='current'||period==='all'||!value)return true;const d=new Date(String(value).slice(0,10)+'T12:00:00'),now=new Date();if(period==='month')return d.getFullYear()===now.getFullYear()&&d.getMonth()===now.getMonth();const days=Number(period);if(!Number.isFinite(days))return true;const min=new Date(now);min.setHours(0,0,0,0);min.setDate(min.getDate()-days+1);return d>=min}
  function prodRows(period){const rows=state.production||[];return period==='current'?rows:rows.filter(r=>inPeriod(r.order_date,period))}
  function finRows(period){const rows=state.receivables||[];return ['current','all'].includes(period)?rows:rows.filter(r=>inPeriod(r.created_at||r.due_date,period))}
  function metric(key,period){const prod=prodRows(period),active=prod.filter(i=>Number(i.quantity_remaining)>0),fin=finRows(period),open=fin.filter(r=>Number(r.amount_remaining)>0);switch(key){case'pending_boxes':return{value:active.reduce((s,i)=>s+Number(i.quantity_remaining||0),0),label:'caixas a produzir'};case'produced_boxes':return{value:prod.reduce((s,i)=>s+Number(i.quantity_produced||0),0),label:'caixas produzidas'};case'production_progress':{const t=prod.reduce((s,i)=>s+Number(i.quantity_ordered||0),0),p=prod.reduce((s,i)=>s+Number(i.quantity_produced||0),0);return{value:t?Math.min(100,Math.round(p/t*100)):0,label:'produção concluída',suffix:'%'}}case'open_orders':return{value:new Set(active.map(i=>i.order_id)).size,label:'pedidos abertos'};case'partial_items':return{value:active.filter(i=>i.production_status==='partial').length,label:'itens parciais'};case'overdue_orders':return{value:new Set(active.filter(isOverdue).map(i=>i.order_id)).size,label:'pedidos atrasados'};case'receivable_amount':return{value:open.reduce((s,r)=>s+Number(r.amount_remaining||0),0),label:'a receber',money:true};case'debtor_customers':return{value:new Set(open.map(r=>r.customer_id)).size,label:'clientes devendo'};case'overdue_amount':return{value:open.filter(r=>r.financial_status==='overdue').reduce((s,r)=>s+Number(r.amount_remaining||0),0),label:'valor vencido',money:true};case'finance_progress':{const t=fin.reduce((s,r)=>s+Number(r.amount||0),0),p=fin.reduce((s,r)=>s+Number(r.amount_paid||0)+Number(r.adjustment||0),0);return{value:t?Math.min(100,Math.round(p/t*100)):0,label:'recebido',suffix:'%'}}default:return{value:0,label:''}}}
  const fmt=m=>m.money?money(m.value):`${m.value}${m.suffix||''}`;
  const periodLabel=p=>({current:'Atual','7':'7 dias','30':'30 dias','90':'90 dias',month:'Este mês',all:'Histórico'})[p]||'Atual';
  function ensureUi(){
    if(!$('#cardCustomizerStyles')){const s=document.createElement('style');s.id='cardCustomizerStyles';s.textContent=`.hero-card .card-eye{position:absolute;right:52px;top:16px;border:0;background:transparent;color:var(--tone);font-size:18px;z-index:5;opacity:.82;padding:2px 5px}.hero-card .card-eye:hover{opacity:1}.hero-card .card-menu{z-index:5}.card-metric-main{font-size:36px;font-weight:900;color:var(--tone);margin:14px 0 3px;letter-spacing:-.04em}.card-metric-label{color:var(--muted);font-size:12px}.card-secondary{margin-top:18px;color:var(--muted);font-size:12px}.card-secondary strong{color:var(--text)}.card-period-chip{display:inline-flex;padding:4px 7px;border-radius:999px;background:color-mix(in srgb,var(--tone) 12%,transparent);color:var(--tone);font-size:9px;font-weight:900;margin-top:8px}.card-config-modal .modal{width:min(680px,100%)}.card-config-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px;padding:18px 20px}.card-config-grid label{display:grid;gap:6px;font-size:11px;font-weight:800}.card-config-grid input,.card-config-grid select{width:100%}.card-config-actions{display:flex;justify-content:space-between;gap:10px;padding:0 20px 18px}.card-config-actions>div{display:flex;gap:8px}.card-add-wrap{display:flex;justify-content:flex-end;margin:8px 0 16px}.card-add-btn{border:1px dashed var(--line);background:transparent;color:var(--muted);padding:9px 13px;border-radius:11px;font-weight:800}.card-add-btn:hover{color:var(--text);border-color:var(--muted)}`;document.head.appendChild(s)}
    if(!$('#cardConfigModal')){const d=document.createElement('div');d.id='cardConfigModal';d.className='modal-backdrop card-config-modal';d.innerHTML=`<div class="modal"><div class="modal-head"><div><span class="eyebrow">PERSONALIZAR CARD</span><h3 id="ccHeading">Card</h3></div><button class="icon-btn" id="ccClose">×</button></div><div class="card-config-grid"><label>Título<input id="ccTitle"></label><label>Cor<select id="ccColor">${Object.entries(COLORS).map(([k])=>`<option value="${k}">${k}</option>`).join('')}</select></label><label>Métrica principal<select id="ccPrimary"></select></label><label>Métrica secundária<select id="ccSecondary"></select></label><label>Período<select id="ccPeriod"><option value="current">Pendências atuais</option><option value="7">Últimos 7 dias</option><option value="30">Últimos 30 dias</option><option value="90">Últimos 90 dias</option><option value="month">Este mês</option><option value="all">Todo o histórico</option></select></label><label>Ao clicar<select id="ccTarget"><option value="">Nenhuma ação</option><option value="orders">Abrir Pedidos</option><option value="finance">Abrir Financeiro</option><option value="customers">Abrir Clientes</option></select></label><label style="display:flex;align-items:center;gap:8px"><input id="ccVisible" type="checkbox" style="width:auto"> Exibir card</label></div><div class="card-config-actions"><button class="btn danger-btn" id="ccDelete" style="display:none">Excluir card</button><div><button class="btn secondary" id="ccCancel">Cancelar</button><button class="btn primary" id="ccSave">Salvar</button></div></div></div>`;document.body.appendChild(d);d.addEventListener('click',e=>{if(e.target===d)d.classList.remove('show')});$('#ccClose').onclick=$('#ccCancel').onclick=()=>d.classList.remove('show')}
    if(!$('#addCustomCard')){const w=document.createElement('div');w.className='card-add-wrap';w.innerHTML='<button id="addCustomCard" class="card-add-btn">+ Criar card</button>';$('#heroCards')?.insertAdjacentElement('afterend',w);$('#addCustomCard').onclick=()=>openConfig(null,true)}
  }
  function fillMetricSelect(el,selected,allowNone=false){el.innerHTML=(allowNone?'<option value="">Nenhuma</option>':'')+Object.entries(METRICS).map(([k,v])=>`<option value="${k}" ${k===selected?'selected':''}>${v}</option>`).join('')}
  function openConfig(id,isNew=false){ensureUi();editing=isNew?{id:'custom-'+Date.now(),title:'NOVO CARD',primary:'pending_boxes',secondary:'',period:'current',color:'blue',visible:true,target:'',moneyHidden:false}:id==='production'?cfg.production:id==='finance'?cfg.finance:cfg.custom.find(c=>c.id===id);if(!editing)return;$('#ccHeading').textContent=editing.title||'Card';$('#ccTitle').value=editing.title||'';$('#ccColor').value=editing.color||'yellow';fillMetricSelect($('#ccPrimary'),editing.primary);fillMetricSelect($('#ccSecondary'),editing.secondary,true);$('#ccPeriod').value=editing.period||'current';$('#ccTarget').value=editing.target||'';$('#ccVisible').checked=editing.visible!==false;$('#ccDelete').style.display=id&&id.startsWith('custom-')?'':'none';$('#ccSave').onclick=()=>{const next={...editing,title:$('#ccTitle').value.trim()||'CARD',color:$('#ccColor').value,primary:$('#ccPrimary').value,secondary:$('#ccSecondary').value,period:$('#ccPeriod').value,target:$('#ccTarget').value,visible:$('#ccVisible').checked};if(next.id==='production')cfg.production=next;else if(next.id==='finance')cfg.finance=next;else{const ix=cfg.custom.findIndex(c=>c.id===next.id);if(ix>=0)cfg.custom[ix]=next;else cfg.custom.push(next)}save();$('#cardConfigModal').classList.remove('show');renderCards()};$('#ccDelete').onclick=()=>{cfg.custom=cfg.custom.filter(c=>c.id!==editing.id);save();$('#cardConfigModal').classList.remove('show');renderCards()};$('#cardConfigModal').classList.add('show')}
  function cardContent(c){const p=metric(c.primary,c.period),s=c.secondary?metric(c.secondary,c.period):null,hasMoney=p.money||s?.money;return `<button class="card-menu" aria-label="Personalizar" title="Personalizar card">•••</button>${hasMoney?`<button class="card-eye" aria-label="Ocultar ou mostrar valor" title="">${c.moneyHidden?'⊘':'◉'}</button>`:''}<div class="hero-copy"><span class="hero-kicker">${esc(c.title||'CARD')}</span><div class="card-metric-main ${p.money?'card-money':''}">${esc(fmt(p))}</div><div class="card-metric-label">${esc(p.label)}</div><span class="card-period-chip">${esc(periodLabel(c.period))}</span>${s?`<div class="card-secondary"><strong class="${s.money?'card-money':''}">${esc(fmt(s))}</strong> · ${esc(s.label)}</div>`:''}</div>`}
  function bindCard(card,c){card.querySelector('.card-menu').onclick=e=>{e.preventDefault();e.stopPropagation();openConfig(c.id)};const eye=card.querySelector('.card-eye');if(eye)eye.onclick=e=>{e.preventDefault();e.stopPropagation();c.moneyHidden=!c.moneyHidden;save();renderCards()};card.querySelectorAll('.card-money').forEach(el=>el.classList.toggle('is-hidden',!!c.moneyHidden||!!state.prefs?.hideMoney));card.onclick=e=>{if(e.target.closest('.card-menu,.card-eye'))return;if(c.target)goPage(c.target)}}
  function renderOneExisting(selector,c){const card=$(selector);if(!card)return;card.className='hero-card';card.style.setProperty('--tone',COLORS[c.color]||COLORS.yellow);card.style.display=c.visible===false?'none':'';card.dataset.cardConfigId=c.id;delete card.dataset.targetPage;delete card.dataset.targetFilter;card.innerHTML=cardContent(c);bindCard(card,c)}
  function renderCards(){ensureUi();renderOneExisting('[data-card="production"]',cfg.production);renderOneExisting('[data-card="finance"]',cfg.finance);$$('[data-custom-card]').forEach(e=>e.remove());const root=$('#heroCards');if(root)cfg.custom.forEach(c=>{const card=document.createElement('article');card.className='hero-card';card.dataset.customCard=c.id;card.style.setProperty('--tone',COLORS[c.color]||COLORS.blue);card.style.display=c.visible===false?'none':'';card.innerHTML=cardContent(c);root.appendChild(card);bindCard(card,c)})}
  const oldRenderHome=window.renderHome;window.renderHome=function(){oldRenderHome?.();renderCards()};
  document.addEventListener('DOMContentLoaded',()=>{ensureUi();setTimeout(renderCards,0)});
})();